services:
  app:
    build: .
    ports:
      - "5173:5173"
    volumes:
      - .:/app
      - /app/node_modules # Isso cria um volume anônimo que isola a pasta do host
    environment:
      - CHOKIDAR_USEPOLLING=true